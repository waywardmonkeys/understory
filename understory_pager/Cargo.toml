[package]
name = "understory_pager"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Keyed discrete navigation primitives for stable, revisitable sequences."
keywords = ["ui", "navigation", "pager", "no_std", "understory"]
categories = ["gui", "data-structures", "no-std"]

[dependencies]

[lints]
workspace = true

[features]
default = ["std"]

# This crate is `no_std` + `alloc` by default; `std` is only needed when
# dependants prefer to compile with the standard library.
std = []

[package.metadata.docs.rs]
all-features = true
default-target = "x86_64-unknown-linux-gnu"
targets = []
